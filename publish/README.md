# LaunchInventoryTidy v3.0.1 release artifact

`LaunchInventoryTidy.zip` is the distributable archive. Extract it into the Unturned installation directory.

```text
BepInEx/
  plugins/
    LaunchInventoryTidy.dll
```

The archive intentionally contains one DLL only. Install `LaunchMultiplayerNet.dll` version 4.0.0 or newer separately in the same plugin directory.

Current package integrity:

| Artifact | SHA-256 |
|---|---|
| `LaunchInventoryTidy.dll` | `2CF2FD3C486BF810067E328C330F11A6A2F45973900F78962FFF73498123AE16` |
| `LaunchInventoryTidy.zip` | `20750CA3E940FF6210F8110B943D15367123FD5EF3EEE12C620BA451D6B12024` |

P2P remains Alpha-only pending dynamic two-machine validation. See the repository README for supported environment and dependency boundaries.
